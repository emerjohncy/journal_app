<h1><%= @task.name %></h1>
<h3><%= @task.description %></h3>
<h3><%= @task.deadline %></h3>

<%= link_to 'Edit', edit_category_task_path(@category.id, @task.id) %>
<%= link_to 'Delete', [@task.category, @task], data: { "turbo-method": :delete, turbo_confirm: "Are you sure?" } %>
<%= link_to 'Back to Tasks', category_tasks_path(@category.id) %>