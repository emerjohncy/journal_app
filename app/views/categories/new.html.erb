<div class="container-fluid vh-100 div-content">

    <%# List of Categories Div %>
    <div class="title">
        Category (<%= current_user.categories.all.count %>)
    </div>
    <div class="main-content col">
        <% current_user.categories.all.each do |category| %>
            <%= render "category", category: category %>
        <% end %>
    </div>

    <%# Add a Category Div %>
    <div class="col">
        <%= render "form", category: @category, url: create_category_path, method: "Add" %>
    </div>

    <% @category.errors.full_messages_for(:name).each do |message| %>
        <div class="error"><%= message %></div>
    <% end %>

</div>